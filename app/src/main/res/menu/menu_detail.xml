<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <group android:id="@+id/favorite_true">
        <item
            android:id="@+id/remove_favorite"
            android:icon="@drawable/ic_grade_red_24dp"
            android:orderInCategory="100"
            android:title="@string/favorite_menu"
            app:showAsAction="ifRoom" />
    </group>
    <group android:id="@+id/favorite_false">

        <item
            android:id="@+id/add_favorite"
            android:icon="@drawable/ic_grade_white_24dp"
            android:orderInCategory="100"
            android:title="@string/favorite_menu"
            app:showAsAction="ifRoom" />
    </group>

</menu>

